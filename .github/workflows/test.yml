name: GHA Test

on:
  push:
    branches:
      - main

jobs:
  Plan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/templates/test-paths
        with:
          who-to-greet: 'Plan'
          action-path: 'plan'
  Apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/templates/test-paths
        with:
          action-path: 'apply'
